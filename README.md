# QUAN Recovery

AI-Powered Micro-Debt Collection Platform

## Quick Start

```bash
# Install dependencies
pip install -e ".[dev]"

# Run the API
python -m quan.main

# Run the dashboard
cd dashboard && npm install && npm run dev
```
